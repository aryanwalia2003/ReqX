# Shared hooks

Hooks used by two or more features — e.g. `useDebounce`, `useEventListener`.
Run `npm run gen:hook` to scaffold one. Feature-specific hooks belong in that
feature's own `hooks/` folder instead.
