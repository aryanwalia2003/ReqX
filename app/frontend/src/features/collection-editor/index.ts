// Public API for the "collection-editor" feature. Only export what other
// features and src/app are allowed to import — everything else in this
// folder is private.
export { CollectionEditorPanel } from './components/CollectionEditorPanel'
