package services

// ExampleService is a template Wails-bound service. Copy this file trio
// (struct/ctor/method) as the starting point when wiring a real feature —
// collections, runs, socket debugging, history — into the desktop app.
type ExampleService struct{}
